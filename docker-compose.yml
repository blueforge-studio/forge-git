version: '3.9'

services:
  gitea:
    image: gitea/gitea:1.21
    container_name: forge-git-gitea
    restart: unless-stopped
    ports:
      - "3001:3000"
      - "2222:22"
    environment:
      - USER_UID=1000
      - USER_GID=1000
      - GITEA__database__DB_TYPE=sqlite3
      - GITEA__database__PATH=/data/gitea/gitea.db
      - GITEA__server__DOMAIN=git.localhost
      - GITEA__server__ROOT_URL=http://git.localhost:3001
      - GITEA__server__SSH_PORT=2222
      - GITEA__mailer__ENABLED=false
    volumes:
      - gitea-data:/data
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - redis

  deploy-runner:
    build:
      context: .
      dockerfile: packages/deploy-runner/Dockerfile
    container_name: forge-git-deploy-runner
    restart: unless-stopped
    environment:
      - REDIS_URL=redis://redis:6379
      - MINIO_URL=http://minio:9000
      - MINIO_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${MINIO_SECRET_KEY}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - redis
      - minio

  postgres:
    image: postgres:16-alpine
    container_name: forge-git-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=forge
      - POSTGRES_PASSWORD=forge
      - POSTGRES_DB=forge-git
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "forge"]
      interval: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: forge-git-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data

  minio:
    image: minio/minio:latest
    container_name: forge-git-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_ACCESS_KEY=${MINIO_ACCESS_KEY}
      - MINIO_ROOT_SECRET_KEY=${MINIO_SECRET_KEY}
    volumes:
      - minio-data:/data

volumes:
  gitea-data:
  postgres-data:
  redis-data:
  minio-data: